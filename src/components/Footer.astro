---
import { getCollection } from "astro:content"
import Container from "./Container.astro"

const year = () => new Date().getFullYear()
const linksPage = await getCollection("pages")

const info = [
	{
		text: 'ТОО "REPAIRSTROYGRAND"',
	},
	{
		text: "БИН: 230540040698",
	},
	{
		text: "Email: rsgvalveab@gmail.com",
		link: "mailto:rsgvalveab@gmail.com",
	},
	{
		text: "Телефон: +7 707 498 9953 (WhatsApp)",
		link: "https://wa.me/77074989953",
	},
]
---

<footer class="bg-blue-700 py-12">
	<Container>
		<div class="flex justify-between gap-3 md:gap-6 md:flex-row flex-col">
			<span class="text-3xl text-white">ЛОГОТИП</span>
			<div class="flex-col flex gap-3">
        {
          
          info &&
            
          info.map((item) => (
              <a class="text-white" href={item.link ?? '#'}>{item.text}</a>
            ))
        }
      </div>
		</div>
	</Container>
</footer>
